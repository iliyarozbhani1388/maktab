name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "GitHub Actions is working!"
          echo "Starting test..."
          df -h
          free -h
          python --version
